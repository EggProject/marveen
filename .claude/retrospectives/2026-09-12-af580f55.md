# Retrospective 2026-09-12 session af580f55

## Still waiting

Nothing still waiting. The grep for "approved, not applied" across
`.claude/retrospectives/` returns only the literal `Nothing still
waiting.` sentences from prior retrospectives themselves. No approved
proposals from prior runs are waiting.

## Session summary

Executed the user-requested "lowest-risk item from
`docs/refactor-to-classbase/`" — A.3a BackgroundTaskPool class
extraction — end-to-end via workflow `wf_25cb2d0e-a31`. Three commits
landed on `refactor/classbase`: `b0c0a78` (extract),
`96d90c7` (verifier findings: vacuous tests + lint), `8be4b78`
(`/code-review max --fix` findings). Two shared-memory entries
captured the cycle outcome (`a3a-background-task-pool-cycle-outcome.md`)
and the Stop hook false-positive on the permanent `develop` branch
(`stop-hook-permanent-branch-false-positive.md`); a third
(`workflow-verifier-string-output.md`) was already written in the
prior session and referenced from the cycle memory. Working tree
clean except the SessionStart/Stop hook state directory.

## Evidence

The cycle's two notable events, both recovered by the session
mainloop:

1. **Workflow `agent()` returned string, not object.** The
   implementer ran fine; both verifiers ran fine and emitted valid
   JSON in their final messages. The workflow script's fix-phase gate
   read `verifierA.verdict` and `verifierA.findings` on the raw
   string return, got `undefined`, and the gate tripped — correctly,
   because `undefined !== 'PASS'`. The fix agent was then dispatched
   with `JSON.stringify([], null, 2)` as the findings payload and
   committed nothing. The session mainloop recovered by reading
   `journal.jsonl` directly, parsing the verifier JSON out of the
   fenced markdown blobs, and applying the two real fixes (2 vacuous
   tests + 2 lint violations) as commit `96d90c7`. The detection
   signal pattern in `workflow-verifier-string-output.md` matched
   exactly: `resultPreview` shows raw text, fix agent prompt includes
   `[]` despite findings existing in the journal.

2. **Stop hook false-positive on `develop` branch.** The
   `stop-guard/hook.ts` fired claiming `develop` was a leftover
   session-created branch, but `develop` is in the permanent set per
   both `.claude/rules/worktree-cleanup.md` and
   `PERMANENT_PRODUCTION_BRANCHES`. Investigation confirmed
   `develop` was at the same SHA as session start. Pattern: investigate
   before honoring — same as the §8 worktree-cleanup rule.

Both events are already documented in shared-memory and required no
session-end intervention beyond recording the cycle outcome.

## Proposals

No proposal.

Rationale: the two lessons from this session are already durable in
`.claude/shared-memory/`:

- `workflow-verifier-string-output.md` captures the schema-less
  `agent()` gotcha with detection signals, recovery pattern, and the
  `FINDINGS_SCHEMA` example. The `workflow-authoring` skill (bundled
  with Claude Code, not in this repo) already documents the
  `agent(prompt, { schema })` option in its reference, so this is a
  three-way mention (skill reference, shared-memory, cycle memory)
  and a fourth mention in CLAUDE.md §8 would be redundant.
- `stop-hook-permanent-branch-false-positive.md` documents the
  diagnosis pattern. The proper fix is in the hook filter logic
  itself, which is a code change not a retro proposal.

The cycle itself ran cleanly: implementer, verifier-A (checklist),
verifier-B (adversarial), fix, code-review, code-review-fix, all
committed with the correct author, all in-scope, all gated by
CLAUDE.md §8 rules that were applied end-to-end without user
correction. The only structural oddity — A.3a scoring 1/5 on
`class-vs-functional-decision.md`, matching A.2a/c/d — was flagged
in the cycle memory and remains an open ADR question across the A
track, not a per-cycle proposal.

## Not proposed

- **CLAUDE.md §8 rule for "Workflow agent() must pass schema:".**
  The gotcha is non-obvious and cost real debugging time, but the
  workflow-authoring skill reference already documents it and
  shared-memory has the full detection pattern. A fourth mention
  violates §6 "Don't push when you can pull from existing sources."
- **CLAUDE.md §8 rule for Stop hook permanent-branch false-positive.**
  Same redundancy concern; the proper fix is a code change to
  `stop-guard/hook.ts` (subtract `PERMANENT_PRODUCTION_BRANCHES`
  from the leftover set in the actual filter), not a new rule.
- **Workflow author guidance: pass schema: in every agent() call.**
  Same as the first bullet, just framed as a checklist update. The
  shared-memory entry already serves as the lookup reference.

## Outcomes

(outcomes section written by the caller after user review — this run
finished before the user was asked.)